References :
https://docs.gitlab.com/ee/gitlab-basics/start-using-git.html
https://help.github.com/en/articles/adding-an-existing-project-to-github-using-the-command-line

# gitcommands

# Add Username and emailaddress
git config --global user.name roshra
git config --global user.email roshmohanr@gmail.com

# Confirm the global username and password is entered
git config --global user.name
git config --global user.email

# Confirm global list
git config --global --list

# Clone a repository
# This can be done via HTTPS or SSH
git clone <http url path to clone>>
git clone <SSH Path>

# To get the latest changes on brach
git checkout master

# To work on upto-date copy 
# from the last instance you cloned or pulled the project 
git pull REMOTE master

# Post Changes to local git repository push to git
# From the local repository which needs push
git add * 
git commit -m "any comment"
git push origin master
git --rebase origin

# Now if you already have a repository on git and wanted to push
<Get repository URL>
Go to the directory on local machine - /FIND/JAVA
Do a clone command 
	git clone <Get repository URL>
Set new remote : 
	git remote -v 
	git add *
	git commit -m "comments"
	git push origin master

